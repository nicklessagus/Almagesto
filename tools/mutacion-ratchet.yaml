# Techo de funciones de `scripts/` que SOBREVIVEN a la mutación (romperlas no pone rojo a ningún
# test). Sólo puede BAJAR. Se mide con `python tools/mutar.py --todo --ratchet`.
#
# Una función acá no está probada: su test —si existe— pasa por construcción. Es el defecto que
# esta sesión encontró cuatro veces (un `assert path.glob(...)` siempre truthy, una guarda cuyo
# test pasaba sin la guarda, un "espera creciente" que `[2.0, 2.0]` satisface, un test de
# determinismo corriendo las dos pasadas en el mismo proceso).
medido_en: "2026-08-27"
alcance: "TODO `scripts/` — 406 funciones, barrido completo (`--todo --ratchet`, 1.68.0, pasada /auditar del 2026-08-27)"
techo: 3

# ⚠ EL TECHO SUBIÓ DE 7 A 10, Y NO ES UNA REGRESIÓN — es la primera medición sobre el árbol
# ENTERO. El 7 se había medido sobre 268 funciones y el campo `alcance` lo llamaba "la mayor parte
# de scripts/": faltaban 60, entre ellas `openalex.py` completo. Las tres que "aparecen" nunca
# estuvieron en la población anterior, así que 7 y 10 no son comparables — son dos mediciones de
# universos distintos, y la regla es declarar la discrepancia en vez de elegir un número
# (regla de método #5). La pasada `/auditar` del 2026-08-24 lo encontró así: el gate salía 1 contra
# un techo que nunca había cubierto lo que decía cubrir (AUD-35), en el mismo archivo donde un
# párrafo afirmaba que el barrido completo "todavía no corrió" mientras el campo de abajo decía que
# sí, sobre 268 (AUD-21).
#
# ⛔ Esto NO habilita subir el techo para poner verde un rojo. La única justificación admisible es
# la de acá: la población cambió y se dice cuál era y cuál es. Con el alcance ya fijado en TODO
# `scripts/`, de ahora en más sólo puede bajar.
#
# BAJÓ DE 8 A 4 EL 2026-08-25, y el número coincide con lo previsto por razones distintas — se
# declara en vez de elegir una versión (regla de método #5). El handoff proyectaba 5 suponiendo que
# se habían cubierto TRES (`tesseract_version`, `defuddle_version`, `sweep_external._run`). Medido:
#   · se cubrieron CUATRO — las tres más `sweep_external.aplicar_ground_truth`;
#   · APARECIÓ UNA NUEVA que nunca estuvo en la lista de 8: `make_notes.py::citas`;
#   · la población pasó de 328 a 331 funciones.
# El barrido dio 5 —el número proyectado— pero por una compensación: cuatro cubiertas y una nueva,
# no tres cubiertas. `citas` es el helper que coerce
# `citation_count` a int en el apéndice `## Excluidos por el filtro`: **pura**, no frontera con el
# entorno — así que la afirmación de la versión anterior de este archivo («todas son fronteras con
# el entorno: no queda ninguna función pura acá») había dejado de ser cierta sin que nada avisara.
# Se cerró con `tests/test_make_notes.py::test_excluidos_coerce_citation_count_no_numerico`, que se
# vio MORIR bajo mutación antes de contarlo (regla de método #3) → 4.
#
# RE-MEDIDO EL 2026-08-25 (1.41.1) sobre 338 funciones: siguen siendo LAS MISMAS 4, así que el
# techo no se mueve. La población creció de 331 a 338 —`lib_config.section_start`,
# `lib_blocks._split_row` y las funciones de `tests/test_section_anchor.py`— y las tres nuevas
# MUEREN bajo mutación, verificadas antes de contarlas (regla de método #3). Se declara el
# cambio de población en vez de comparar dos universos distintos (regla de método #5).
#
# LAS 4, medidas sobre 338 funciones. Todas son fronteras con el entorno (red, subprocess, locale):
#   citation_index.py::_fetch_ads_default   ← ADS `fl=reference`: red
#   citation_index.py::_fetch_oa_default    ← idem OpenAlex
#   fetch_web.py::fetch                     ← `npx defuddle`: subprocess contra Node
#   lib_config.py::stdout_tolerante         ← shim de encoding del stdout (locale del OS)
#
# CUBIERTAS EN LA TANDA 1.38.x (salieron de la lista de 8): `extract_fulltext.tesseract_version`,
# `fetch_web.defuddle_version`, `sweep_external._run` y `sweep_external.aplicar_ground_truth`.
#
# BAJÓ DE 10 A 8 EN LA MISMA SESIÓN: los dos `_mailto` (Crossref y OpenAlex) eran las únicas de las
# diez que no necesitan red —leen `git config`— y son gemelas, así que las mata **un solo test
# parametrizado** (`tests/test_mailto_paridad.py`), que es la red #2 aplicada: si N módulos prometen
# la misma forma se prueba una vez, no con prosa en N docstrings. El test parametriza además el
# valor de ausencia, que es la única diferencia real entre las gemelas (`None` para Crossref, `""`
# para OpenAlex): si alguien las "unifica", el test lo dice.
#
# LAS PRÓXIMAS: las 4 que quedan son todas red o subprocess. Bajarlas pide o un doble en la frontera
# (inyectar el `subprocess.run`, como se hizo con `_mailto`) o un smoke test contra el servicio real
# —que es la regla de método #1: un cliente de red probado sólo contra la red falseada valida que el
# CLIENTE funcione, no que el CONTRATO se cumpla.

# 2026-08-25 (INV-100/101). Lo MEDIDO: `tools/mutar.py scripts/extraction_prompt.py` da **0
# sobrevivientes** sobre sus 5 funciones nuevas, así que el techo NO se mueve — siguen siendo las
# mismas 4. Lo NO medido, y se declara en vez de estimarlo: el barrido `--todo` no se re-corrió, así
# que la población de 338 es la de 1.41.1 más 5 funciones nuevas **contadas, no barridas**. Sumar y
# escribir 343 sería presentar aritmética como medición (regla de método #5).
#
# ⚠ ALCANCE QUE ESTE ARCHIVO **NO** CUBRE, y conviene decirlo: `archivos_del_diff` filtra por
# `scripts/`, así que `tools/mutar.py` —el gate mismo— nunca se muta. Se descubrió al arreglar
# INV-101, que vive justamente ahí. No es una regresión (el alcance siempre dijo `scripts/`), pero
# ahora que `tools/` entró al mapa de trazabilidad la asimetría queda a la vista: el código que
# decide qué se muta es el único que no se muta.

# 2026-08-27 · pasada `/auditar` (issues #141 y #167). Dos cosas, y conviene no mezclarlas:
#
# (a) EL ALCANCE ESTABA MAL DECLARADO. El campo decía «338 funciones … hoy hay 5 más sin barrer»
#     (= 343) y el propio contador del gate cuenta **406**: 63 funciones no barridas y no
#     declaradas. Es el análogo exacto de AUD-35, que este mismo archivo documenta arriba — un
#     techo que se lee como medido sobre `scripts/` entero y estaba medido sobre el 84 % de él.
#     Se declara la discrepancia (regla de método #5) y el alcance queda fijado en la población
#     COMPLETA, que es la única forma de que de acá en más sólo pueda bajar.
#
# (b) EL TECHO BAJÓ DE 4 A 3, y esto NO es lo mismo que (a): el barrido completo sobre las 406
#     midió **3 sobrevivientes**, o sea por debajo del techo vigente. `extraction_prompt.py::main`
#     salió de la lista. Un techo por encima de lo medido deja de ser ratchet y se vuelve
#     decorativo, así que se baja.
#
#     Los 3 que quedan son todos RED (`citation_index._fetch_ads_default`,
#     `citation_index._fetch_oa_default`, `fetch_web.fetch`): bajarlos pide un doble en la frontera
#     o un smoke test contra el servicio real (regla de método #1), no un test más de los que ya hay.
#
# ⚠ `extraction_prompt.py::main` sigue en `tools/cobertura-ratchet.yaml` (sin ejecutar) y salió de
#    acá (su mutación muere). No es contradicción: son ejes distintos —«nadie la corre» vs «romperla
#    no pone rojo a nadie»— y una función puede correr de refilón y aun así ser mutable sin ruido.
