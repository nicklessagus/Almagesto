# Techo de tamaño de `CLAUDE.md`. Sólo puede BAJAR. Se mide con `pytest tests/test_doc_size.py`.
#
# QUÉ ES ESTO. `CLAUDE.md` se inyecta ENTERO en el contexto de cada sesión de cada instancia: es el
# único documento del repo cuyo tamaño cuesta en cada operación. El 2026-08-30 medía 2258 líneas /
# 201 KB, y el 73 % de sus bytes era color-desarrollo (postmortems de issues, mediciones con corpus,
# «hasta 1.x este documento decía») — la cuarta copia de material que ya vive en los issues públicos,
# `docs/mediciones.md` y `docs/contrato.md`. El recorte lo bajó a lo de abajo; este ratchet impide
# que vuelva a crecer por acreción.
#
# LA REGLA DE ESCRITURA que este techo mecaniza: CLAUDE.md lleva **la regla y su consecuencia en una
# línea, con su ancla (#N/D-N)**; el caso que la produjo y la medición van al issue y a
# `docs/mediciones.md`. Idea tomada de agentic-memory-vault (límite de tamaño como regla forzada),
# implementada como ratchet al estilo `idioma-ratchet.yaml`.
#
#   - por ENCIMA del techo → el test falla: alguien inlineó color-desarrollo. La salida normal es
#     mover el porqué a su issue/mediciones.md y dejar la regla con su ancla.
#   - por DEBAJO con margen (>3 %) → pasa y avisa que hay que bajar el techo (un techo que nadie
#     ajusta se vuelve decorativo — AUD-07).
#
# ⛔ SUBIR EL TECHO SE PUEDE, Y NUNCA EN SILENCIO (#340). Hasta 1.150.0 el techo era un muro: sólo
# bajaba. Eso funcionó mientras hubiera postmortem que mover — pero medido el 2026-08-31, sólo el
# **3 %** de CLAUDE.md (43 de 1620 líneas, 4161 de 128011 B) lleva marca de postmortem, y las tres
# secciones que el plan de recorte marcaba para una segunda pasada tienen 696 B entre las tres. O
# sea: el archivo ya está en su densidad de diseño, y «sólo baja» se había convertido en «no se
# puede escribir ninguna regla nueva». Medido: en la tanda #329-#335, TRES fixes decidieron no
# escribir su regla por falta de espacio, en silencio y sin registro.
#
# Ahora el techo es un AVISO con firma: se puede subir, y cada suba deja su entrada en
# `crecimientos` con fecha, ancla y motivo. Lo que sigue prohibido es que el archivo crezca sin que
# nadie lo declare — que es lo que el ratchet vino a frenar. `test_la_suba_del_techo_esta_declarada`
# lo vigila: el techo vigente tiene que ser el de la última entrada.
#
# ⛔ TECHO Y PRESUPUESTO (#465). Medido el 2026-09-14: 43 subas firmadas y 0 rechazadas en 14 días,
# el archivo clavado contra el techo (7 líneas libres) — el mismo estado que #340 midió al abrir la
# puerta. Cada firma se juzgaba sola («¿entra esta regla?», y la respuesta honesta casi siempre es
# sí), ninguna contra la tendencia, y ninguna declaraba qué sale. Tres cosas nuevas:
#   - `sale:` es obligatorio en toda entrada de `crecimientos` desde esa fecha: qué sale de
#     CLAUDE.md para hacer lugar, o por qué nada puede salir (mismo criterio que `--reason`).
#   - `pendiente:` es una ALARMA (aviso, no rojo): N subas en una ventana móvil de D días.
#   - `skills:` da techo a cada `SKILL.md` — entran ENTEROS al contexto al invocarse, misma economía
#     que CLAUDE.md, y nadie los miraba (los 4 grandes: 13 kB el 1-jul → 185 kB el 14-sep). Una suba
#     se firma en `crecimientos_skills`; un skill nuevo declara su techo al nacer.
#   - Y la red barata del hallazgo secundario: claves `ancla:` contadas contra las entradas
#     parseadas, o una entrada sin `- fecha:` se anida en la anterior y la firma se pierde en
#     silencio (pasó con #345: 44 `ancla:`, 43 entradas).
medido_en: "2026-09-22"
techo_lineas: 1322
techo_bytes: 99451

pendiente:            # aviso, no rojo: N subas firmadas dentro de una ventana móvil
  ventana_dias: 14
  max_subas: 6

sale_obligatorio_desde: "2026-09-14"

skills:               # techo en bytes por SKILL.md (#465); se ajusta como el de CLAUDE.md
  append-knowledge: 12200
  audit-note: 18500
  auditar: 18400
  find-contradictions: 21500
  ingest-star: 32000
  ingest-theme: 51800
  maintain: 36803
  query-corpus: 6000
  setup: 15300
  test-hypothesis: 15700
  verify-citations: 66500

crecimientos_skills:      # {fecha, skill, ancla, motivo, sale, techo_bytes} por suba
  - fecha: "2026-09-17"
    skill: audit-note
    ancla: "#490"
    motivo: >-
      El paso 4 mandaba re-verificar lo corregido y no decía que tres clases de defecto del
      corrector se deciden ANTES, sobre el diff: medido en una auditoría de tres notas, el corrector
      introdujo 14 defectos, el `lint` vio 0 y el fan-out a ciegas 11 — o sea tres rondas de
      subagentes para lo que un `grep` sobre las líneas agregadas contesta. Un comando que el skill
      no nombra no lo corre nadie (es el defecto que #323 midió sobre `--validar`).
    sale: >-
      La mitad de lo que escribí primero: la explicación de las tres categorías pasó de un párrafo
      con sus porqués a una cláusula con sus nombres, porque el porqué ya vive en el issue y en
      `contrast.preflight`. Del resto del skill no sale nada: se revisó y sus siete frentes son la
      operación misma, no color — sacar uno cambiaría lo que la auditoría cubre, no cómo se cuenta.
    techo_bytes: 18500
  - fecha: "2026-09-14"
    skill: append-knowledge
    ancla: "#463"
    motivo: >-
      Dos líneas: el bloqueante de #353 tiene una cuarta salida cuando el equivocado es el
      CATÁLOGO, y este skill es uno de los dos lugares donde un operador declara una fuente
      off-ADS y se la come. Sin el puntero, la salida existe y no la encuentra quien la necesita
      (#436).
    sale: >-
      Nada. El párrafo de #392 ya está en regla-y-consecuencia y las dos líneas son el puntero
      más corto posible: el comando y su ancla, con el porqué en `docs/lint.md` § #463.
    techo_bytes: 12200

  - fecha: "2026-09-20"
    skill: maintain
    ancla: "#494"
    motivo: >-
      La operación que cierra la deuda de paginación no existía, y `maintain` es el skill que la
      manda a resolver: hasta acá el sub-modo decía «al re-leer esas páginas se actualizan los
      localizadores y se saca la marca» sin ningún comando detrás, o sea una instrucción que nadie
      podía seguir sobre 68 extracciones y 1653 localizadores. Entra la cadena de cuatro comandos
      —paquete, fan-out para LEER, escritor SERIAL, re-anclaje— y la regla que la gobierna: la
      página del `.txt` es GUÍA de dónde abrir y la hoja es la que responde. La mecánica de una
      operación va a su skill (#465), y sin ella el backlog más grande del repo queda nombrado y
      sin salida ejecutable, que es lo que #436 ya había corregido del otro lado.
    sale: >-
      El párrafo que explicaba en prosa qué había que hacer al re-leer, reemplazado por el bloque
      de comandos que lo hace; y la versión larga de este mismo bloque se recortó a la mitad antes
      de firmar (las tres garantías del escritor —testigo de la hoja, hueco declarado, ronda
      parcial— pasaron de un párrafo con sus porqués a una cláusula con sus nombres, porque el
      porqué vive en el issue, en `docs/mediciones.md` y en el docstring de `repaginate.py`).
    techo_bytes: 36803

crecimientos:
  - fecha: "2026-08-31"
    ancla: "#340"
    motivo: >-
      El techo quedó clavado (1620/1620 líneas, 489 B libres) y tres fixes de la tanda #329-#335
      resolvieron por omisión no escribir su regla. Medido que no queda postmortem para mover (3 %
      del archivo), así que el muro dejaba de proteger y empezaba a decidir. Se abre lugar para las
      reglas de #341 (la marca cuando dos lecturas del PDF discrepan) y #342 (el hueco declara su
      alcance), las dos pedidas por el usuario.
    techo_lineas: 1640
    techo_bytes: 130500
  - fecha: "2026-08-31"
    ancla: "#351"
    motivo: >-
      El techo quedó a UNA línea (1639/1640) y la regla de #351 —el tema de método que hereda
      `database:astronomy` en silencio, con la puerta fundacional cerrada por otro lado— no entra
      sin subirlo. No queda postmortem que mover (#340 lo midió: 3 % del archivo), y el caso ya
      costó una re-síntesis entera del tema `ica`. Suba mínima: 5 líneas.
    techo_lineas: 1645
    techo_bytes: 130300
  - fecha: "2026-08-31"
    ancla: "#344"
    motivo: >-
      La tabla de verificación pasa a un hermano `<nota>.verif.md` (el 71-77 % de los bytes de una
      nota de entidad, medido) y eso trae una regla nueva que hay que saber ANTES de escribir una
      nota: dónde vive el rastro, qué queda en la nota, y el invariante de par que el lint bloquea
      en cuatro categorías. No hay postmortem que mover para hacerle lugar (medido en #340: 3 % del
      archivo), y la regla no puede vivir sólo en `docs/`: un agente que escribe el bloque lee esto.
    techo_lineas: 1660
    techo_bytes: 131600
  - fecha: "2026-08-31"
    ancla: "#344"
    motivo: >-
      Ajuste del MERGE, no una regla nueva: #351 y #344 subieron el techo cada uno por su cuenta en
      ramas paralelas, y al integrarlas conviven las dos reglas — la del `search_fq` heredado y la
      del par nota↔`.verif.md`. El archivo quedó en 1661/131731 contra el techo de 1660 de #344, que
      no había visto la línea de #351. Suba mínima para que las dos entren.
    techo_lineas: 1665
    techo_bytes: 132200
  - fecha: "2026-08-31"
    ancla: "#345"
    motivo: >-
      El alcance de las redes 1 y 4 es una REGLA ESCRITA de CLAUDE.md («toda función nueva de
      `scripts/`»), así que meter `tools/` adentro se tiene que escribir ahí y no en tests/README.md:
      el documento que define qué pasa antes de cerrar un issue no puede decir un alcance y la
      herramienta correr otro. Se buscó primero el lugar dentro del techo vigente (1639 de 1640
      líneas: no había). Lo que entra es la regla, su consecuencia y la exención declarada de
      `tools/refresh_issues.py` con su ancla; la medición y el postmortem van a docs/mediciones.md.
    techo_lineas: 1652
    techo_bytes: 131000
  - fecha: "2026-08-31"
    ancla: "#345"
    motivo: >-
      Ajuste del MERGE, como el de #344: #345 y la integración de #351/#344 subieron el techo cada
      uno en su rama, y al juntarlas conviven las tres reglas nuevas — el `search_fq` heredado, el
      par nota↔`.verif.md` y el alcance de las redes 1 y 4 con su exención declarada. El archivo
      quedó en 1669/132565 contra el techo de 1665, que no había visto la regla de #345. Suba
      mínima para que las tres entren.
    techo_lineas: 1672
    techo_bytes: 132900
  - fecha: "2026-09-03"
    ancla: "#390"
    motivo: >-
      Invierte una regla que ya estaba escrita, así que no alcanza con borrar la vieja: hay que
      decir que el driver de `merge=ours` se pasa POR COMANDO y por qué registrarlo destruye contra
      `origin` — sin el porqué, el próximo operador lo vuelve a registrar siguiendo el README de
      cualquier instancia vieja. Medido en repos sintéticos: con el driver puesto, lo que trae la
      otra máquina desaparece sin conflicto. La regla ya está comprimida a 5 líneas y se recuperó
      una arreglando la promesa falsa del `git pull` a secas, que este mismo cambio invalida.
    techo_lineas: 1676
    techo_bytes: 133200
  - fecha: "2026-09-03"
    ancla: "#386"
    motivo: >-
      Dos reglas que un agente necesita ANTES de escribir una entrada de `log.md`: que la exención
      de la marca `⚠ corregido` la decide una sola función que los dos chequeos llaman —una
      convención en prosa no compone, y ésta ya había divergido— y que la entrada que cita una cita
      defectuosa para explicarla va en blockquote, que es el caso reflexivo sin el cual la regla se
      muerde la cola. Comprimidas dos veces antes de subir; el caso medido vive en #386 y #387.
    techo_lineas: 1682
    techo_bytes: 133800
  - fecha: "2026-09-03"
    ancla: "#373"
    motivo: >-
      Entra una CAPA entera a la superficie de chequeo —las citas de `## Vista`, 3838 transcripciones
      directas del PDF que no miraba ninguna capa— y con ella la regla que un agente necesita antes
      de escribir una vista: el bibcode de la nota se SUMA a los adyacentes, no cede ante una
      mención vecina. Comprimida dos veces antes de subir; el caso medido vive en #373.
    techo_lineas: 1683
    techo_bytes: 134000
  - fecha: "2026-09-03"
    ancla: "#359"
    motivo: >-
      Un paso NUEVO en el mapa del camino del texto —el cosechador cruza cada cita del JSON contra
      el `.txt`—, y ese mapa es lo que un agente lee para saber qué chequea cada eslabón. Una línea,
      comprimida dos veces; el caso medido (2 de 265 valores de una ingesta real, las dos nacidas en
      la extracción) vive en #359.
    techo_lineas: 1684
    techo_bytes: 134100
  - fecha: "2026-09-04"
    ancla: "#385"
    motivo: >-
      Tres reglas de ESCRITURA de la síntesis que un agente necesita antes de redactar prosa citada,
      y las tres salieron de medir un cierre real: el fragmento pegable para prosa (#385, 33 de 43
      citas se transcribían a mano), agrupar N fuentes bajo un mecanismo exige haberlo visto en las
      N (#370), y corregir por sustracción antes que por reemplazo (#389, 3 de 15 defectos nacieron
      corrigiendo, los 3 con material agregado). Dos líneas cada una; el caso medido vive en su issue.
    techo_lineas: 1690
    techo_bytes: 134700
  - fecha: "2026-09-04"
    ancla: "#383"
    motivo: >-
      Tres reglas de la tanda de artefactos, cada una de una línea y comprimida dos veces: el
      renombre re-apunta los punteros por verdad de disco (#356), `--fix-key` para la clave errónea
      que no es alias (#355), y el reemplazo de un PDF deja `pdf_source` en null vía `pdf_sha`
      (#383) — más el campo `pdf_sha` en la lista del schema de `papers/`. El caso medido vive en
      cada issue.
    techo_lineas: 1692
    techo_bytes: 135200
  - fecha: "2026-09-04"
    ancla: "#357"
    motivo: >-
      Cinco reglas de la tanda de temas de método entraron comprimiendo cada vez (#354, #360, #384,
      #382: dos líneas o menos cada una, con dos compresiones vecinas por regla); la de #357 —contra
      QUÉ contador compara la puerta 2, y que bajar el umbral no es la salida— es lo que un agente
      necesita saber ANTES de acordar `fundacional_min_citas` con el usuario, y no entra sin subir.
      Medido en `icasso`: 21/21 en tema y 0 core con cualquier umbral. No queda postmortem que mover
      (#340 lo midió). Suba mínima: 4 líneas.
    techo_lineas: 1696
    techo_bytes: 135500
  - fecha: "2026-09-04"
    ancla: "#392"
    motivo: >-
      Regla TRANSVERSAL pedida por el usuario con todas las letras («en todos lados hay que agregar
      no respondas de memoria»): D-59 la enunciaba sólo para el fan-out de verify, y donde el agente
      ESCRIBE identidad no regía. Medido en la instancia: 4 de 29 fuentes declaradas con autor
      inventado por asociación con el tema, seis días como verdad, con la planilla correcta al
      lado del PDF. Va en CLAUDE.md porque es la regla que un agente necesita ANTES de escribir
      cualquier campo identificatorio, en cualquier skill; los skills llevan el comando concreto.
      Comprimida dos veces; el techo estaba a 6 bytes. Suba: 7 líneas.
    techo_lineas: 1703
    techo_bytes: 136200
  - fecha: "2026-09-04"
    ancla: "AUD-237"
    motivo: >-
      La lista «Bloqueantes (0 para cerrar)» de §Lint omitía 14 de las 40 categorías que
      `scripts/lint.py` declara `SEV_BLOQUEANTE` (AUD-237, AUD-385…394): un agente que la usara
      como checklist cerraba con bloqueantes vivos. Entran las 14 en la misma lista, más la
      política `relevance.chain_autoaccept` (AUD-235: en un tema el chaining no pasa por triage) y
      la unidad real del fan-out de verify (AUD-325). Se comprimió cuatro veces —intro de «las
      nueve redes», bullets de `discover`, D-48, redes 1 y 9—: los bytes entran (136181/136200) y
      falta UNA línea. Suba de una línea, sin bytes.
    techo_lineas: 1704
    techo_bytes: 136200
  - fecha: "2026-09-05"
    ancla: "#394"
    motivo: >-
      El techo estaba clavado (1704/1704) y la regla de #394 no entraba: el lint tiene que usar la
      MISMA resolución de candidatos que `contrast` desde #373, y el bibcode propio SUMA en vez de
      reemplazar. Es regla de escritura de nota —quien sintetiza necesita saber por qué una cita de
      la propia `## Vista` no se juzga contra el vecino—, así que su lugar es CLAUDE.md y no sólo
      `docs/lint.md`. Se escribió comprimida (3 líneas: regla, consecuencia medida y ancla); el caso
      completo —5 de 19 hallazgos falsos, 3 verbatim en el `.txt` propio— vive en el issue.
    techo_lineas: 1707
    techo_bytes: 136453
  - fecha: "2026-09-05"
    ancla: "#395"
    motivo: >-
      La lente que una vista declara la escribe ahora el PROMPT, no el cosechador, y eso cambia una
      regla de escritura que ya estaba en CLAUDE.md (INV-146): dejar la vieja habría sido peor que
      no tenerla, porque describe un mecanismo que dejó de existir. Van cuatro líneas: quién la
      escribe, por qué no se deriva de las claves de `ejes` (son lo contestado, y tomarlas encoge el
      denominador de #270) y el migrador. La medición —209 slots, 48 ejes sin línea, 13 vistas mal
      juzgadas— vive en el issue.
    techo_lineas: 1711
    techo_bytes: 136890
  - fecha: "2026-09-05"
    ancla: "#397"
    motivo: >-
      Campo nuevo del schema de `papers/` (`bibtex` + su procedencia y su fecha) y una regla de
      escritura que no puede vivir sólo en `docs/`: el BibTeX se TRAE, no se redacta, y el hueco es
      un estado correcto. Quien escribe una nota o un informe necesita saberlo antes de rearmar una
      entrada a mano, que es exactamente lo que el issue midió. La medición —81 entradas del `.bib`,
      39 resueltas, 8 papers con id distinto entre instancias— vive en el issue.
    techo_lineas: 1714
    techo_bytes: 137807
  - fecha: "2026-09-05"
    ancla: "#398"
    motivo: >-
      Cambia lo que un lector VE en toda nota de paper sin leer: la sección de la vista pasa del
      prompt al extractor a una línea de estado. Es una regla de escritura de nota —qué dice la
      sección mientras la lectura no ocurrió, y que el prompt tiene una sola casa—, así que no
      alcanza con `docs/lint.md`. Ocho líneas; la medición (47 notas, 46 bajo `no_vista`) vive en
      el issue.
    techo_lineas: 1722
    techo_bytes: 138451
  - fecha: "2026-09-05"
    ancla: "#397"
    motivo: >-
      Una línea: `bibtex` sin `bibtex_source` entra a la lista de bloqueantes, que el propio archivo
      declara como «la lista `SEV_BLOQUEANTE` de scripts/lint.py: si agregás una, va acá». Omitirla
      es el hallazgo AUD-385…394 de la auditoría anterior, cometido de nuevo el mismo día que se
      cerró.
    techo_lineas: 1723
    techo_bytes: 138555
  - fecha: "2026-09-05"
    ancla: "#391"
    motivo: >-
      La regla del `log` cambia y la vieja no se puede dejar: decía «la entrada refutada se MARCA,
      no se edita» y hoy la marca no exime nada. Las cinco líneas de más son el porqué, que acá es
      la regla misma —por qué una cita no va al `log`— más el aviso de qué pasó con la marca, sin
      el cual una instancia con 8 entradas marcadas no entiende por qué le volvieron a salir.
    techo_lineas: 1728
    techo_bytes: 138908
  - fecha: "2026-09-06"
    ancla: "#403"
    motivo: >-
      La cadena de verify-citations tenía generador, barrera y re-anclaje, y el eslabón que escribe
      en la bóveda —el hermano `.verif.md`— lo redactaba el agente en cada corrida (medido: 4 veces,
      2 mal, una de ellas 117 pares «vencidos por fuente» falsos). Ahora es un script, y la regla
      «el hermano lo escribe el script, no el agente» va en el paso 6 del camino del texto. Entró
      en las mismas dos líneas del paso: +16 bytes sobre el techo, cero líneas.
    techo_lineas: 1728
    techo_bytes: 138924
  - fecha: "2026-09-06"
    ancla: "#409"
    motivo: >-
      La red 10 («¿quién MÁS lleva esta regla?»). No es una regla más: es la que cierra la familia
      de defectos MÁS GRANDE del repo, medida sobre los ~400 issues por cuatro lectores
      independientes —el fix escrito contra el caso medido y no contra la relación que lo contiene,
      entre el 30 % y el 50 % de cada tramo—. Va en la lista de redes porque ahí es donde se busca
      qué correr antes de cerrar, y no cabía en el techo anterior: +12 líneas /
      +1063 bytes.
    techo_lineas: 1740
    techo_bytes: 139987
  - fecha: "2026-09-06"
    ancla: "#409"
    motivo: >-
      La red 10 quedó escrita como herramienta y le faltaba el momento: la regla que dice CUÁNDO se
      corre —al reportar y al cerrar un issue— vive en §Framework vs instancia, que es donde una
      instancia lee qué hacer con un hallazgo de framework. Sin eso el gate sale 0 en un repo donde
      nadie enumeró nada, que es el falso limpio de D-43 un nivel más arriba. Pedida por el usuario
      con todas las letras («que lo incorpore como regla general cada vez que tiene que reportar o
      cerrar errores»). Ocho líneas; la medición vive en #409.
    techo_lineas: 1749
    techo_bytes: 140734
  - fecha: "2026-09-06"
    ancla: "#415"
    motivo: >-
      Dos reglas de ESCRITURA que un agente necesita antes de declarar una fuente o de mirar el
      hueco de un abstract, las dos medidas en la bóveda real. #415: el item de `sources:` declara
      `pdf_source`, que es el único campo del schema que decide lecturas y que para esa población
      NO es re-derivable (38 de 57 notas con PDF sin procedencia). #413: el `## Abstract` se llena
      desde el catálogo y no sólo desde el PDF, o el placeholder es permanente justo donde el
      abstract es todo lo que la nota tiene (29 con placeholder, 10 recuperables). Cinco líneas
      cada una; el caso completo vive en su issue.
    techo_lineas: 1759
    techo_bytes: 141662
  - fecha: "2026-09-06"
    ancla: "#417"
    motivo: >-
      Cinco líneas sobre la regla que la primera versión de #413 rompió y que un agente necesita
      antes de correr el flag o de declarar una fuente sin abstract: se reemplaza el PLACEHOLDER y
      nunca el encabezado (9 de 9 notas llenadas pasaron a violar un bloqueante), y
      `sin_abstract_motivo` es el simétrico de `sin_conclusiones` — «la fuente no lo tiene» no es
      «falta la copia de catálogo», y el catálogo devuelve igual el arranque de la introducción.
    techo_lineas: 1764
    techo_bytes: 142102
  - fecha: "2026-09-06"
    ancla: "#424"
    motivo: >-
      Campo nuevo del schema de `papers/` (`data_availability`) y sección estampada nueva en la
      ficha (`## Datos públicos`): un agente que escribe una nota necesita saber que el puntero a
      los datos va donde el paper lo AFIRMA —citable, con página, verificable— y no en
      `data_local`, que es machine-local y no viaja. Doce líneas; el caso medido (4 tablas en una
      sola estrella, que dejaban de tener dónde vivir al arreglar #421) vive en el issue.
    techo_lineas: 1776
    techo_bytes: 143062
  - fecha: "2026-09-09"
    ancla: "#430"
    motivo: >-
      El techo quedó clavado (1776/1776) y la regla de #430 no entra sin subirlo. Es regla de
      ESCRITURA, no postmortem: un agente que edita el bloque de verificación a mano tiene que
      saber que su línea se lee normalizada, que el fragmento se reconoce por la plantilla que lo
      escribe, y que la prosa del triage que entra no puede salir vacía —porque el modo de falla es
      silencioso y lo que se pierde es el único registro de qué decidió la ronda (10 sub-secciones
      en 4 notas, 2 ya con dos conteos contradictorios)—. Se comprimió tres veces (5 → 4 líneas)
      antes de firmar; el caso medido vive en el issue. La cuarta línea la pagó un hallazgo del
      propio fix: la red tiene que ir por SECCIÓN y no por sub-sección, porque la que pasa por el
      lector no ve el nombre mal escrito — y eso cambia lo que un agente tiene que saber.
    techo_lineas: 1780
    techo_bytes: 143500
  - fecha: "2026-09-09"
    ancla: "#427"
    motivo: >-
      Dos líneas: la clase de la condición va UNA vez y la resolución la escribe un script. Es
      regla de escritura —el agente que cierra una nota decide ahí si edita la celda a mano— y su
      consecuencia no es cosmética: duplicada, la fila queda irresoluble y el conteo que la nota
      publica sobre sí misma miente para siempre (41 de 957 celdas medidas, 7 de ellas `acota`).
      Se comprimió dos veces antes de firmar; el caso medido vive en el issue.
    techo_lineas: 1782
    techo_bytes: 143700
  - fecha: "2026-09-09"
    ancla: "#428"
    motivo: >-
      Una línea, dentro del mapa «el camino del texto de punta a punta»: `--from` es repetible y el
      ancla muerta se descarta sólo DECLARADA. Va acá porque ese mapa es lo que un agente lee antes
      de armar el bloque de verificación, y el caso medido es que la herramienta admitía UN solo
      orden y no tenía salida cuando ese orden se rompió — le pasó dos veces en la misma sesión,
      con el error ya identificado y anotado en el `log`.
    techo_lineas: 1783
    techo_bytes: 143800
  - fecha: "2026-09-09"
    ancla: "#429"
    motivo: >-
      La matriz método × estrella pasa a ser el CUARTO roll-up estampado, y eso cambia una regla de
      escritura: dejó de ser curación del agente y no espeja `methods_applied.literature` (los dos
      campos no tienen clave de join — medido, 23 de 42 métodos declarados sin ningún paper que
      matchee), así que llenarla a mano publicaba huecos FALSOS y el `—` necesita alcance declarado
      (D-34). Cinco líneas. Se comprimió primero: el porqué de #60 estaba duplicado en el
      blockquote de `index.md` y en la sección de roll-ups, que es su casa — quedó el puntero
      (-3 líneas). El caso medido vive en el issue.
    techo_lineas: 1781
    techo_bytes: 143443
  - fecha: "2026-09-09"
    ancla: "#429"
    motivo: >-
      Ajuste del MERGE, no una regla nueva: #427/#428/#430 y #429 subieron el techo cada uno por su
      cuenta en ramas paralelas —el segundo midió contra una base que no tenía las líneas del
      primero— y al integrarlas conviven las cuatro reglas. El archivo queda en 1788/144109 contra
      el techo de 1783 de #428, que no había visto la línea de #429. Suba mínima para que las
      cuatro entren. (Mismo caso que el ajuste de #344 del 2026-08-31.)
    techo_lineas: 1788
    techo_bytes: 144200
  - fecha: "2026-09-11"
    ancla: "#433 + #434 + #435 + #436"
    motivo: >-
      Dos reglas de escritura nuevas, las dos medidas cerrando categorías enteras en la instancia.
      #433: el cruce de SEGUNDA MANO se acredita nombrando al dueño o se FIRMA revisado —un detector
      recall-only sin salida mide su tasa de disparo y no la deuda (leídos 66 hallazgos uno por uno,
      48 seguían listados, 40 de ellos coincidencias numéricas que ninguna prosa cierra)—. #434: la
      resolución de una condición se direcciona por el PAR `(ancla, bibcode)` y no por el ancla, que
      hashea el bloque (5 de 20 `acota` irresolubles por compartir ancla con una `contextualiza`).
      #435: cada categoría de la superficie de propuestas declara DÓNDE aterriza su firma y si el
      barrido la cruza (60 de 62 propuestas eran permanentes: el productor leía un artefacto
      versionado y no regenerable mientras la firma aterrizaba en otro lado). Quince líneas entre
      las tres, cada una donde ya vive su tema; el caso medido y el postmortem viven en los issues
      y en `docs/mediciones.md`. #436: el backlog más grande del repo (161 de 264 notas) gana su
      comando —`replace_pdf.py`— y la regla que lo funda («todo backlog que el lint nombra tiene una
      salida ejecutable»), más las dos cosas que el comando NO hace: re-verificar y re-paginar la
      extracción. Veintiséis líneas entre las cuatro, cada una donde ya vive su tema; el caso medido
      y el postmortem viven en los issues y en `docs/mediciones.md`.
    techo_lineas: 1814
    techo_bytes: 146650
  - fecha: "2026-09-11"
    ancla: "#437"
    motivo: >-
      Tres líneas en el párrafo de #436: el reemplazo de PDF FIRMA `pdf_reemplazo` en la nota
      (el docstring lo prometía y no se escribía) y AVISA si el entrante tiene menos páginas
      (medido en la instancia: 7 sin Supplementary contra 33, con la ficha citando §S1.1). Es la
      regla que cambia lo que el agente hace al reemplazar, y va donde ya vive el comando. Y tres
      más en el párrafo de `contrast --validar`: la extracción de un PDF reemplazado no acusa sola
      («cambiada» se mide contra el PDF en disco), decidido con el usuario.
    techo_lineas: 1818
    techo_bytes: 147150
  - fecha: "2026-09-12"
    ancla: "#446"
    motivo: >-
      Cuatro issues de la instancia en una tanda (#445-#448), todos de la familia «una sola
      implementación de la regla y los otros lectores no la llaman»: la firma del reemplazo gana
      sobre `build/` (#446, cinco líneas junto a #383, donde ya vive la precedencia de
      `pdf_source`), los slugs de un bibcode son la UNIÓN de PDF y `.txt` (#448), el delta de
      re-clasificación de un tema aplica la lente del tema (#447) y «prosa cambiada» se decide por
      las anclas (#445). Cada una cambia lo que el agente hace y va al párrafo de su regla madre.
    techo_lineas: 1840
    techo_bytes: 149500
  - fecha: "2026-09-13"
    ancla: "#449"
    motivo: >-
      Diez líneas en el párrafo de #213 (las salvedades sobre el ARTEFACTO), donde ya vive la regla
      madre: la salvedad que dice QUÉ DOCUMENTO hay en disco se cruza contra el disco. Es la más
      decidible que escribe el extractor —tres testigos máquina-legibles— y la única que un
      `replace_pdf` deja falsa en cada corrida (medido: 43 salvedades en 31 notas, con lint,
      contrast y verify los tres en verde). Cambia lo que el agente hace al reemplazar y al leer
      una salvedad, así que va acá y no sólo en `docs/lint.md`. La regla de #450 entró en el
      párrafo de #232 sin sumar techo.
    techo_lineas: 1860
    techo_bytes: 151300
  - fecha: "2026-09-13"
    ancla: "#451"
    motivo: >-
      Siete líneas en el párrafo de #221/#427 (la columna `Condición`), donde ya vive la regla
      madre. Es la mitad que faltaba de #450 una columna más allá: rige el ÚLTIMO eslabón, y la
      ronda que trae una condición distinta se encadena en vez de perderse (medido en la instancia:
      55 condiciones `acota` de una ronda de 425 pares no llegaron a su fila, una de ellas diciendo
      que la nota se contradice consigo misma, bajo una celda que publicaba «resuelta»). Cambia lo
      que el agente ve al cerrar una ronda —una fila que vuelve a contar como pendiente—, así que va
      acá y no sólo en `docs/lint.md`. Suma también el re-angostado de #449, que entró en su propio
      párrafo.
    techo_lineas: 1866
    techo_bytes: 151600
  - fecha: "2026-09-13"
    ancla: "#452"
    motivo: >-
      Ocho líneas en los dos párrafos que ya son su casa: el tercer tipo de `SALVEDAD_TIPOS`
      (#213) y la salida del detector de #449. Es la regla que cierra esa categoría: la salvedad
      que dice QUÉ DOCUMENTO se leyó se emite ESTRUCTURADA, porque en prosa el detector tiene que
      adivinar de quién habla la oración —midió 5 hallazgos con precisión 0/5 sobre 268 notas, y el
      cuarto era VERDADERO y sobre el PDF de otro bibcode—. Cambia lo que el extractor escribe y lo
      que el agente corre para migrar lo viejo, así que va acá y no sólo en `docs/lint.md`.
    techo_lineas: 1874
    techo_bytes: 152400
  - fecha: "2026-09-13"
    ancla: "#452 (2ª vuelta)"
    motivo: >-
      Once líneas en el mismo párrafo, por la devolución de la instancia: el migrador propuso mal en
      4 de 6 casos reales y creaba un backlog de 51 notas con salida para 4. Las cuatro reglas que
      lo cierran cambian lo que el agente lee de la propuesta —la clase la decide la CLÁUSULA, la
      negación es simétrica, `web` es una clase, y el proponente cruza el disco y saltea la nota sin
      PDF—, así que van con la regla y no sólo en `docs/mediciones.md`.
    techo_lineas: 1886
    techo_bytes: 153600
  - fecha: "2026-09-13"
    ancla: "#453"
    motivo: >-
      Nueve líneas en el mismo párrafo: el comando con el que se COBRA una salvedad estructurada sin
      re-cosechar la vista. Sin él, la única salida era `--force`, que re-fecha la lectura (medido:
      `fecha` y `lente` de una vista del 2026-08-31 reescritas a la de hoy sobre una lectura que no
      volvió a ocurrir), y por eso las 50 propuestas de #452 no tenían camino a la nota. Es un
      comando que el agente corre, así que va con la regla.
    techo_lineas: 1896
    techo_bytes: 154600
  - fecha: "2026-09-13"
    ancla: "#453 (2ª y 3ª vuelta)"
    motivo: >-
      Diez líneas en el mismo párrafo, por dos devoluciones seguidas de la instancia sobre el mismo
      comando. La primera: el barrido dejó 27 notas afirmando lo que sus testigos desmienten y 50
      `⚙ verificada` desaparecidas (la guarda miraba la FORMA del bloque, no su contenido). La
      segunda, que es la regla que de verdad protege: una salvedad en prosa ya escrita NO se
      reescribe —el cruce anterior comparte ancla con el detector de #449, así que sólo veía las
      frases que ese detector sabe mirar, y las correcciones reales («esta vista se leyó del…») no
      anclan—. Medido: 28 salvedades reescritas en 65 notas, una de ellas una cita textual que la
      fuente no dice. Cambia lo que el agente puede correr sin revisar, así que va con la regla.
    techo_lineas: 1906
    techo_bytes: 155600
  - fecha: "2026-09-13"
    ancla: "#454 + #453 (4ª vuelta)"
    motivo: >-
      Ocho líneas en dos párrafos que ya existen. (a) #454: la extracción es testigo de lo que el
      SINTETIZADOR re-tipeó, nunca de lo que la MÁQUINA copió de ella —el bloque de salvedades lo
      estampa el cosechador desde el JSON, así que el paso 2 lo aprobaba siempre y una cita que la
      fuente no dice se publicó con lint rc 0—. (b) #453 4ª vuelta: el bloque pelado pre-#213 se
      MIGRA en vez de agregarse abajo (medido: 25 notas duplicadas, con el lint en lockstep). Las
      dos cambian lo que el agente puede correr sin revisar.
    techo_lineas: 1914
    techo_bytes: 156600
  - fecha: "2026-09-13"
    ancla: "#455"
    motivo: >-
      Siete líneas en el párrafo de D-26, junto al preview (#208) y al delta (#447), que son los
      otros dos portadores de la misma regla: la re-clasificación del tema va sobre el corpus
      COMPLETO, porque entre medio suman registros la segunda pasada por fecha y el chaining, que
      clasifican con la lente global (medido: 7 de 10 papers de `query:recent` core con
      `puertas: []` —radar, EEG, motores—, bajados y con nota). Cambia lo que el agente ve al
      correr un ingest de tema, así que va con la regla.
    techo_lineas: 1922
    techo_bytes: 157500
  - fecha: "2026-09-13"
    ancla: "#456 + #457 + #458"
    motivo: >-
      Catorce líneas para tres issues de la misma tanda, cada una en el párrafo de su regla madre.
      (a) #456: `pdf_leido` son DOS ejes —qué hay en disco y de qué se leyó la vista—, porque en un
      PDF reemplazado no había valor correcto y el hecho verdadero no era expresable (27 salvedades
      medidas). (b) #457: el `$` suelto de la prosa que va a una nota se escapa, como el `|` de una
      celda (#240). (c) #458: `query_ads` PAGINA y el registro guarda lo traído además de lo pedido
      —ADS topea en 2000 por request, así que «subí --rows» era un no-op sobre un universo de 6964—.
      Las tres cambian lo que el agente escribe o corre.
    techo_lineas: 1936
    techo_bytes: 158800
  - fecha: "2026-09-14"
    ancla: "#465"
    motivo: >-
      BAJA, no suba: salen a `docs/desarrollo.md` las reglas de método 1-3, la convención de idioma
      y las diez redes (−103 líneas, −7,9 kB): se leen al escribir código del framework, que es
      operación explícita del template, y una instancia (#377) las pagaba en cada sesión sin
      usarlas. Entran seis líneas de puntero y el criterio de admisión (siete líneas), que es lo que
      faltaba para que la próxima regla sepa si va acá o a `docs/`. Medido en #465: 43 subas
      firmadas y 0 rechazadas en 14 días con el techo a 7 líneas.
    sale: >-
      Reglas de método 1-3, «Convención de idioma del código» y «Al escribir código: las diez
      redes» → `docs/desarrollo.md`, con puntero desde `CLAUDE.md` y desde `tests/README.md`.
    techo_lineas: 1835
    techo_bytes: 150000
  - fecha: "2026-09-14"
    ancla: "#463"
    motivo: >-
      Seis líneas en el párrafo de `check_sources` (#353), que es su regla madre: cuando el
      equivocado es el CATÁLOGO se firma con `metadata_revisada`, y la firma cubre un ESTADO y no
      un campo. Va acá y no en `docs/` porque decide lo que un agente ESCRIBE en `sources:` y
      porque, sin ella, el bloqueante no tenía salida y todo commit de la bóveda pasaba por
      `--no-verify` (medido en la instancia, #463).
    sale: >-
      Nada. Se buscó: el párrafo de #353 ya está en regla-y-consecuencia (cuatro líneas para el
      cruce entero) y la regla nueva se comprimió de 10 líneas a 6 sacándole la medición, que va al
      issue y a `docs/mediciones.md`. La sección entera de `sources:` mide 14 líneas y sus cuatro
      reglas tienen consumidor distinto: ninguna es candidata a salir sin perderse.
    techo_lineas: 1841
    techo_bytes: 150500
  - fecha: "2026-09-15"
    ancla: "#467 + #466"
    motivo: >-
      Ocho líneas en el párrafo de `bibtex` (#397), su regla madre. (a) #467: el hueco se declara
      con `sin_bibtex` —sexto campo de la familia del motivo obligatorio—, porque el motivo se
      tiraba y la nota consultada se veía igual que la que nadie tocó (17 de 272, lint en rc 0).
      (b) #466: el filtro server-side no puede repetir el criterio que se compara normalizado. Las
      dos cambian lo que el agente ESCRIBE en una nota de paper y lo que lee del reporte.
    sale: >-
      Nada, y esta vez se buscó dos veces: la primera redacción entraba en 10 líneas y se comprimió
      a 8 sacando la medición de #466 (va a `docs/mediciones.md`) y la mención de
      `sin_abstract_motivo`, que ya está nombrada en su propio párrafo. El de #397 son cinco líneas
      y sus tres reglas tienen consumidor distinto.
    techo_lineas: 1849
    techo_bytes: 151300
  - fecha: "2026-09-15"
    ancla: "#471"
    motivo: >-
      Cuatro líneas en el párrafo de `bibtex` (#397), su regla madre: la exportación se pide en la
      forma en que se pega (`journalformat: 3`), el bloque con macro de AASTeX no está cerrado y NO
      se post-procesa. Decide lo que un agente hace al ver un `journal = {\aap}` en una nota —la
      tentación natural es una tabla macro → nombre, que es redactar un campo de la cita— y cómo
      lee el backlog nuevo del lint.
    sale: >-
      Nada. Se buscó: la medición (126 de 219) y la verificación contra la API van a
      `docs/mediciones.md` y a `docs/lint.md`; el párrafo de #397 queda en regla-y-consecuencia
      (cinco reglas con consumidor distinto: procedencia, hueco declarado, dos etapas del DOI,
      forma pegable). Ninguna es candidata a salir sin perderse.
    techo_lineas: 1852
    techo_bytes: 151700
  - fecha: "2026-09-16"
    ancla: "#473"
    motivo: >-
      Diez líneas que reemplazan las cuatro de #471 en el mismo párrafo. La regla de #471 no
      cambia: cambia que tiene TRES síntomas y que cada uno pide una acción distinta, y es
      exactamente lo que un agente necesita antes de tocar un `bibtex` — si trata el cascarón como
      la macro publica una cita que imprime VACÍA, y si trata el mes como la macro fabrica una
      deuda que ninguna corrida puede cerrar. La tabla de consecuencias (`BIBTEX_NO_PEGABLE`) no
      se puede leer del reporte del lint: decide antes.
    sale: >-
      La mitad del párrafo de #471. La versión vieja gastaba tres líneas en explicar la macro y el
      porqué del no-post-procesado; ahora la macro es UN ejemplo de `pendiente` y el «no se
      post-procesa» quedó en media línea, con el porqué apoyado en #397, que está arriba. La
      medición (13 warnings + 2 errores compilando 259 entradas) entra en una cláusula porque es lo
      que justifica que la regla tenga tres ramas; el detalle por forma va a `docs/mediciones.md` y
      el catálogo de las dos categorías nuevas, a `docs/lint.md`.
    techo_lineas: 1862
    techo_bytes: 152700
  - fecha: "2026-09-19"
    ancla: "#492"
    motivo: >-
      Tres líneas que vuelven DECIDIBLE la regla que ya estaba: «con la página del PDF» no dice
      cuál de las dos numeraciones, y el extractor elegía por paper — medido, 44 de 190
      localizadores de una nota en el índice del PDF, consistentes por extracción e inconsistentes
      por bóveda, más 12 mal en la misma nota cerrada con `audit-note` y 239/239 `soportada`. Es lo
      que un agente necesita ANTES de escribir la fila: el número que anota es el que el consumidor
      copia a su `\citep[p.~N]`, y desde #492 hay un chequeo que lo contrasta contra el `.txt`.
    sale: >-
      Nada, y el motivo es que la suba ES el recorte que corresponde: la regla vieja ocupaba media
      línea y no se podía cumplir de una sola manera. El caso, la medición completa y los cuatro
      estados del chequeo van al issue, a `docs/mediciones.md` y a `docs/contrato.md` (INV-155); acá
      queda la convención y su escotilla, que es lo único que decide antes de escribir.
    techo_lineas: 1865
    techo_bytes: 152930
  - fecha: "2026-09-20"
    ancla: "#497"
    motivo: >-
      El CUARTO valor del vocabulario cerrado de `SALVEDAD_TIPOS` (`nota_estado` + `literal` +
      `presente`), en la misma enumeración donde ya viven los otros tres. Es lo que un extractor
      necesita ANTES de escribir una salvedad: si lo que va a decir es sobre LA NOTA y no sobre el
      PDF o el `.txt`, va estructurado y un `grep` lo chequea. Sin el valor en la lista, la única
      forma de escribirlo es la prosa, que es exactamente lo que el issue mide como el agujero:
      ninguna capa la mira y además envejece sola, porque se anota PORQUE algo está mal y la
      extracción es inmutable (#311).
    sale: >-
      Nada, y el motivo es que la suba son 85 bytes en una línea que ya existe: agregar un valor a
      un vocabulario cerrado no tiene parte redundante que sacar. Se intentó pagarlo quitando la
      glosa de `bibcode` opcional del `pdf_leido` de al lado y se revirtió: ese dato NO está
      repetido en el párrafo de #452/#456 —dice que los dos ejes son `documento` y `leido`, no que
      la salvedad puede hablar del PDF de otro bibcode—, así que sacarlo cambiaba 85 bytes por una
      regla de schema perdida. El porqué medido (1497 salvedades en prosa, 149 sobre la nota, el
      caso nominado que NO reproduce y el que sí) va al issue y a `docs/mediciones.md`; el detalle
      del chequeo y su trampa de auto-aprobación, al docstring de `_check_note_state`.
    techo_lineas: 1865
    techo_bytes: 153015
  - fecha: "2026-09-22"
    ancla: "#499"
    motivo: >-
      El DECLARANTE de la tercera fecha de D-12, en el párrafo donde ya viven las tres. Es lo que un
      agente necesita antes de correr `--reanclar` o de leer un encabezado: que el arrastre se
      escribe como sufijo del MISMO encabezado, que la fecha de verificación no se mueve y que el
      lint compara contra el sufijo. Sin eso, la única lectura disponible de un
      `(2026-09-20 · re-anclado 2026-09-22)` es que alguien re-fechó la verificación, que es
      exactamente lo contrario de lo que dice.
    sale: >-
      Nada, y el motivo es que la suba son dos líneas en el párrafo que ya enumera las tres fechas:
      la de síntesis ya declara ahí su declarante (`cfg.save_sintesis`) y esto es su simétrico para
      la de verificación — sacarlo de una para pagar la otra dejaría el párrafo diciendo que dos
      fechas se declaran y una no se sabe. El caso, la medición (9 de 14 notas de entidad, 1499
      pares, `0 re-ancladas`) y las dos salidas descartadas van al issue, a `docs/mediciones.md` y a
      `docs/contrato.md` (INV-82); el reparto por modo (`--from` borra, `--resolver` conserva), al
      docstring de `write_verif_sidecar`.
    techo_lineas: 1867
    techo_bytes: 153218
  - fecha: "2026-09-22"
    ancla: "AUD-475"
    motivo: >-
      BAJA, no suba: recorte de CLAUDE.md de 1867 líneas / 153192 B a 1322 / 99451 B (−35 % de
      bytes). La alarma de pendiente (#465) marcaba 24 subas firmadas en 14 días, cada una juzgada
      sola. Cada ⛔ queda como regla + consecuencia + ancla en una o dos líneas; salen el caso que la
      produjo, las mediciones con sus números, la historia «hasta 1.x», la mecánica fina de las
      operaciones y la enumeración de bloqueantes del lint.
    sale: >-
      Todo lo recortado se MOVIÓ, verbatim como estaba en v1.307.1, a `docs/operacion.md` §
      «Apéndice A — el porqué largo de `CLAUDE.md`», con los mismos encabezados que CLAUDE.md un nivel
      más abajo; CLAUDE.md apunta ahí desde el encabezado y desde el criterio de admisión. El
      catálogo de bloqueantes sigue en `docs/lint.md` § Bloqueantes.
    techo_lineas: 1322
    techo_bytes: 99451
