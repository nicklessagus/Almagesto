# Techo de funciones de `scripts/` que la suite NO ejecuta ni una vez.
#
# Sólo puede BAJAR. Se mide con `pytest tests/poblada/test_cobertura.py -m poblada` (~11 s).
# Una función acá no es "mal probada": es **no mirada por nadie**, y su mutación siempre sobrevive
# —así que esto es el detector barato del mismo defecto que caza `tools/mutar.py`.
medido_en: "2026-08-24"
techo: 4

# Las 6 medidas, y por qué. **Todas** son rutas de RED o subprocess: los tests inyectan dobles
# siempre, así que el código que de verdad habla con las APIs nunca corre. Es deuda declarada.
#   citation_index.py::_fetch_ads_default     ← ADS `fl=reference`, sólo probado a mano
#   citation_index.py::_fetch_oa_default      ← idem OpenAlex
#   fetch_web.py::defuddle_version            ← `npx defuddle`: subprocess contra Node
#   fetch_web.py::fetch                       ← idem
#   sweep_external.py::_run                   ← lanza un subprocess
#   sweep_external.py::aplicar_ground_truth   ← idem (`_run` con --force)
#
# HISTORIAL. El techo fue 11 hasta la pasada `/auditar` del 2026-08-24, que lo encontró vencido:
# se medían 6 contra un techo de 11 y la lista enumeraba once nombres de los cuales cinco ya
# estaban cubiertos —incluidos los dos que el comentario llamaba "los más fáciles de bajar"
# (`_listify_curado`, `_concept_dest`)—. Un techo que nadie ajusta deja de ser ratchet y se vuelve
# un número decorativo (AUD-07). Ya no quedan funciones puras acá: las cuatro que lo eran salieron
# con el test que faltaba.

# 2026-08-26 (#104): 6 → 4. `extraction_prompt.py::main` y `fetch_web.py::fetch` siguen
# adentro; los dos que salieron fueron cubiertos con test, no amnistiados. `discover.py`
# entró con sus tres funciones nuevas ya cubiertas (`_row`, `hydrate`, `main`): la red las
# marcó al crearlas y se cerraron en la misma tanda.
