# Dependencias Python de los scripts de ingesta (scripts/).
# Instalar: pip install -r requirements.txt
# Dependencias de sistema (NO pip): pdftotext (poppler-utils) y git-lfs; opcionales: tesseract
# (OCR de escaneos), curl (fallback de fetch_pdf para publishers con WAF) y Node/npx (sólo para el
# modo off-ADS por web: fetch_web usa `npx defuddle`). Ver docs/operacion.md.
pyyaml>=6.0
requests>=2.28
astroquery>=0.4.6   # fetch_ground_truth.py: NASA Exoplanet Archive + SIMBAD (trae astropy, numpy)

# --- desarrollo (la suite; no hacen falta para usar la bóveda) ---
pytest>=7.0
pytest-xdist>=3.0   # `pytest -n auto`: tier 0 20 s → 10 s, tier 1 2 min → 46 s (medido 2026-08-28)
