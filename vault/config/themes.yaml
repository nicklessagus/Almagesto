# Temas de ESTA bóveda (uno por slug). Archivo de INSTANCIA: está en `merge=ours`
# (ver `.gitattributes`), así que lo que escribas acá sobrevive a cada
# `git merge upstream/main` y el template nunca te lo pisa.
#
# ⛔ El SCHEMA no vive acá: vive en `vault/config/themes.example.yaml` (#376).
#    Ese archivo SÍ es framework, así que baja actualizado con cada merge. Este,
#    al ser `merge=ours`, congelaría el contrato en la versión del día que
#    instanciaste — que es exactamente el defecto que #376 arregla.
#
# Empezá copiando una entrada del `.example` y editándola.

